{
  "name": "parcel-typescript-template",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "parcel ./src/index.html -p 8888 --open",
    "build": "parcel build ./src/index.html --out-dir build",
    "bundle": "npm run clean:build & npm run build",
    "watch": "parcel watch ./src/index.html",
    "clean": "npm run clean:cache & npm run clean:build & npm run clean:dist",
    "clean:build": "rimraf ./build",
    "clean:cache": "rimraf .cache",
    "clean:dist": "rimraf ./dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/pixi.js": "^4.8.0",
    "autoprefixer": "^9.5.0",
    "node-sass": "^4.11.0",
    "parcel-bundler": "^1.12.3",
    "postcss-modules": "^1.1.0",
    "rimraf": "^2.6.2",
    "typescript": "^3.3.4000"
  },
  "dependencies": {
    "pixi.js": "^4.8.2"
  }
}
